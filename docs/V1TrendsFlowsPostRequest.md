# V1TrendsFlowsPostRequest

## Properties

Name | Type | Description | Notes
------------ | ------------- | ------------- | -------------
**bet_type** | Option<**BetType**> | Filter by bet type. If omitted, returns mixed singles and parlays. (enum: singles, parlay, same_game_parlay) | [optional]
**betting_market_category_id** | Option<**i64**> | Deprecated — use `betting_market_category_ids` instead (e.g. Shooting Props, Game Props etc.). A bare `betting_market_category_id` is treated as `betting_market_category_ids: [betting_market_category_id]`. | [optional]
**betting_market_category_ids** | Option<**Vec<i64>**> | Filter by one or more betting market category IDs (e.g. Shooting Props, Game Props etc.). Empty array = no filter. | [optional]
**betting_market_entity_type** | Option<**BettingMarketEntityType**> | Filter by betting market entity type (enum: player, team, event) | [optional]
**betting_market_ids** | Option<**Vec<i64>**> | Filter by betting market IDs | [optional]
**betting_market_position_id** | Option<**i64**> | Deprecated — use `betting_market_position_ids` instead. A bare `betting_market_position_id` is treated as `betting_market_position_ids: [betting_market_position_id]`. | [optional]
**betting_market_position_ids** | Option<**Vec<i64>**> | Filter by one or more betting market position IDs. Empty array = no filter. | [optional]
**event_id** | Option<**i64**> | Deprecated — use `event_ids` instead. A bare `event_id` is treated as `event_ids: [event_id]`. | [optional]
**event_ids** | Option<**Vec<i64>**> | Filter by one or more event IDs. Empty array = no filter. | [optional]
**event_start_date_from** | Option<**chrono::DateTime<chrono::FixedOffset>**> | Filter events starting on or after this value. Accepts a date ('2026-04-19') or full ISO 8601 timestamp ('2026-04-19T00:00:00Z'). | [optional]
**event_start_date_to** | Option<**chrono::DateTime<chrono::FixedOffset>**> | Filter events starting on or before this value. Accepts a date ('2026-04-26') or full ISO 8601 timestamp ('2026-04-26T23:59:59Z'). | [optional]
**fact_flow_type** | Option<**FactFlowType**> | Sub-type for fact flows. Defaults to 'base' if omitted. (enum: base, expanded, multi) | [optional]
**flow_type** | Option<**FlowType**> | Type of flow to return. 'plain' only returns parlays. (enum: fact, fun, plain) | [optional]
**focus_entity_type_id** | Option<**i64**> | Filter by focus entity type ID | [optional]
**full_hit_rate** | Option<**bool**> | When true, include full hit rate data in response | [optional]
**include_alt_lines** | Option<**bool**> | When false, exclude alt lines. Defaults to true. | [optional]
**include_deeplinks** | Option<**bool**> | When true, returns operator-specific odds and deeplink URLs (bet_ios_deep_link_url, bet_android_deep_link_url, bet_web_deep_link_url) for each flow. Requires exactly one operator_id in operator_ids. | [optional]
**include_only_basic_trends** | Option<**bool**> | When true, return only basic trends (omit fact flows with conditions) | [optional]
**include_star_sign_content** | Option<**bool**> | When true, include star sign/horoscope content in flows | [optional]
**league_id** | Option<**i64**> | Deprecated — use `league_ids` instead. A bare `league_id` is treated as `league_ids: [league_id]`. | [optional]
**league_ids** | Option<**Vec<i64>**> | Filter by one or more league IDs. Empty array = no filter. | [optional]
**league_key** | Option<**String**> | League external key (e.g. 'nba', 'united-states.nba'). Format: {league_key} or {country_key}.{league_key}. Alternative to league_id. If both are provided, league_id takes precedence. | [optional]
**likelihood_type** | Option<**LikelihoodType**> | Filter by likelihood category (enum: likely, possible, longshot) | [optional]
**max_hit_rate_threshold** | Option<**i32**> | Maximum hit rate threshold filter (0-100) | [optional]
**max_implied_probability_threshold** | Option<**f64**> | Maximum implied probability threshold filter | [optional]
**min_hit_rate_threshold** | Option<**i32**> | Minimum hit rate threshold filter (0-100) | [optional]
**min_implied_probability_threshold** | Option<**f64**> | Minimum implied probability threshold filter | [optional]
**odds_format** | Option<**OddsFormat**> | Odds display format. Defaults per product_mode (dfs→multiplier, prediction_market→percent, else american). (enum: american, decimal, fractional, multiplier, percent) | [optional]
**operator_ids** | Option<**Vec<i64>**> | Filter by operator IDs | [optional]
**operator_keys** | Option<**Vec<String>**> | Filter by operator external keys (e.g. 'draftkings'). Format: {operator_key}. Resolved IDs are merged with operator_ids. | [optional]
**page** | Option<**i64**> |  | [optional]
**page_size** | Option<**i64**> |  | [optional]
**player_id** | Option<**i64**> | Deprecated — use `player_ids` instead. A bare `player_id` is treated as `player_ids: [player_id]`. | [optional]
**player_ids** | Option<**Vec<i64>**> | Filter by one or more player IDs. Empty array = no filter. | [optional]
**player_key** | Option<**String**> | Player external key (e.g. 'nikola-vucevic', 'chicago-bulls.tre-jones', 'nba.boston-celtics.nikola-vucevic'). Format: {player_key} or {team_key}.{player_key} or {league_key}.{team_key}.{player_key}. Alternative to player_id. If both are provided, player_id takes precedence. | [optional]
**product_mode** | Option<**ProductMode**> | Display mode. dfs rewrites stat lines to MORE/LESS; on the paginated flows endpoints it also auto-filters to over/under player props (entity_type=player, positions [4,5]) unless an entity/position filter is set. (enum: sportsbook, dfs, prediction_market) | [optional]
**split_type** | Option<**SplitType**> | Filter by split type (overs or unders) (enum: overs, unders) | [optional]
**sport_id** | Option<**i64**> | Deprecated — use `sport_ids` instead. A bare `sport_id` is treated as `sport_ids: [sport_id]`. | [optional]
**sport_ids** | Option<**Vec<i64>**> | Filter by one or more sport IDs. Empty array = no filter. | [optional]
**sport_key** | Option<**String**> | Sport external key (e.g. 'american-football'). Format: {sport_key}. Alternative to sport_id. If both are provided, sport_id takes precedence. | [optional]
**starting_soon** | Option<**bool**> | When true, only return flows for events starting soon | [optional]
**team_id** | Option<**i64**> | Deprecated — use `team_ids` instead. A bare `team_id` is treated as `team_ids: [team_id]`. | [optional]
**team_ids** | Option<**Vec<i64>**> | Filter by one or more team IDs. Empty array = no filter. | [optional]
**team_key** | Option<**String**> | Team external key (e.g. 'new-england-patriots', 'nfl.new-england-patriots'). Format: {team_key} or {league_key}.{team_key}. Alternative to team_id. If both are provided, team_id takes precedence. | [optional]
**team_split** | Option<**String**> | Team split filter value | [optional]
**use_cartoon_images** | Option<**bool**> | When true, the logo fields on each flow, leg, and multi-trend slot (default_logo_url, logo_url_1, logo_url_2) are replaced with cartoon-jersey image URLs derived from the relevant team, player, or league. When false or omitted, the original logo URLs are returned. Defaults to false. | [optional]

[[Back to Model list]](../README.md#documentation-for-models) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to README]](../README.md)


